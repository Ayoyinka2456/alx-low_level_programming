#include "main.h"

/**
 * _islower - Checks for lowercase
 *@c: the character to check
 * Return: 0 always successful and 1 if lowercase is found.
 *
 */

int _islower(int c)
{

	return  (c >= 'a' && c <= 'z');
}
