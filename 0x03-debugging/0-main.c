#include "main.h"

/**
 * main - To test if the
 * positive_or_negative function works
 *
 *  Result: 0 if successful, 1 otherwise
 */

int main(void)
{
	int i = 0;

	positive_or_negative(i);

	return (0);
}
